using Microsoft.Quantum.Simulation.Core;
using Microsoft.Quantum.Simulation.Simulators;

namespace SimonProblem
{
    class Driver
    {
        // f(x) = f(x bitxor a) for arbitrary x, try to determine a 
        static void Main(string[] args)
        {
            
        }
    }
}